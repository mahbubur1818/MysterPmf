blog1= {
    title:"Extract text from image using python",
    content:'<h1 class="title">Extract text from image using python program with tesseract</h1><p>Extract text from image is known as OCR (Optical Character Recognition). Python-tesseract is an optical character recognition (OCR) tool for Python </p><p>For Windows download the tesseract from <a href="https://github.com/UB-Mannheim/tesseract/wiki">here</a> </p><p>You must have python installed in your computer. Then install these modules</p><div class="code">pip install opencv-python<br>pip install pytesseract<br>pip install tesseract</div><p>First import necessary modules</p><div class="code">import cv2<br>import pytesseract</div><p>Then read the image and add the location of the tesseract</p><div class="code">img = cv2.imread("quotes.jpg")<br>pytesseract.pytesseract.tesseract_cmd = "C:/Program Files/Tesseract-OCR/tesseract.exe"</div><p>Then convert the image to string using the image_to_string() method . And print the output. The complete code is here.</p><div class="code">import cv2<br>import pytesseract<br><br># reading image<br>img = cv2.imread("pic.jpg")<br><br># configurations is optionalconfig = ("-l eng --oem 1 --psm 3")<br>pytesseract.pytesseract.tesseract_cmd = "C:/Program Files/Tesseract-OCR/tesseract.exe"<br>text = pytesseract.image_to_string(img, config=config)<br><br># printing output text<br>print(text)<br></div>'
  }
blog2 = {
  title:"Convert text to Audio using python",
  content:'<h1>Convert text to Audio using python</h1><p>This program will convert any text into human-like speech. There are two ways to do it. </p><h2>Using Google text to speech (online)</h2><p>We acn use gTTS (Google Text To Speech) to convert text to audio.First install the gTTS and playsound module.</p><div class="code">pip install gtts <br />pip install playsound</div><p>The complete code is shown below.</p><div class="code">import gtts <br />from playsound import playsound <br />tts = gtts.gTTS("Hi friends, I am Pmf","en") <br /># Here the second parameter is language.tts.save("output.mp3") <br />playsound("output.mp3") <br /></div><p>If you want to see all the supported language execute the following command. </p><div class="code">print(gtts.lang.tts_langs())</div><h2>2. Using pyttsx3 library (offline)</h2><p>It looks for TTS engines pre-installed in your machine and uses them. </p><p>First, import the pyttsx3 library and initialize the engine. </p><div class="code">import pyttsx3<br>engine = pyttsx3.init()</div><p>Use say() and runAndWait() methods to convert a text into Speech.</p><div class="code">text=input("Enter your text")<br>engine.say(text)<br>#Now play it using<br>engine.runAndWait()</div><p>We can also save it in a file. The complete code is shown here.</p><div class="code">import pyttsx3<br>engine = pyttsx3.init()<br>text=input("Enter your text")<br>engine.say(text)<br>engine.save_to_file(text, "python.mp3")<br>engine.runAndWait()</div>'
}
blog = [blog1,blog2]


module.exports = blog;